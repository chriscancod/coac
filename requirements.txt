# Core agent
langgraph>=0.2.0
langgraph-checkpoint-postgres>=0.1.0
openai>=1.30.0

# Web server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Database
asyncpg>=0.29.0

# HTTP (APNs requires HTTP/2 — httpx + h2)
httpx[http2]>=0.27.0
h2>=4.1.0

# APNs JWT signing
PyJWT>=2.8.0
cryptography>=42.0.0

# Scheduling
APScheduler>=3.10.0

# Pydantic (FastAPI peer dep — explicit for Railway pin)
pydantic>=2.7.0
